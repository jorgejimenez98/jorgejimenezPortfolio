const baseUrl = 'http://localhost:5000/api'

const urls = {
    configurations: `${baseUrl}/getSiteConfigurations/`,
    sendEmail: `${baseUrl}/sendEmail/`,
    expertises: `${baseUrl}/expertises/`,
    socialMedia: `${baseUrl}/social_medias/`,
}

export default urls